import React from 'react';
import "./NoFoundMessage.scss"

const NoFoundMessage = () => {
    return (
        <div className="NoFoundMessage">
            По вашему запросу ничего не найдено.
        </div>
    );
};

export default NoFoundMessage;